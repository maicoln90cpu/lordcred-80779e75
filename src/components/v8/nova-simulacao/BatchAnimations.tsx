/**
 * BatchAnimations — Reexporta indicadores de realtime/animação usados no fluxo
 * de Nova Simulação. Mantém um único ponto de import para os sub-componentes
 * desta pasta. Não contém lógica nova — apenas agrupa.
 */
export { RealtimeFreshness, AutoRetryIndicator } from '../V8RealtimeIndicators';
export { AnimatedCountBadge } from '../V8AnimatedCountBadge';
